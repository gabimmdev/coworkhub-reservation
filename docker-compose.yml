services:
  db:
    image: postgres:16
    container_name: coworkhub_db
    environment:
      POSTGRES_DB: coworkhub
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - dbdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d coworkhub"]
      interval: 5s
      timeout: 5s
      retries: 10

  app:
    build: .
    container_name: coworkhub_app
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: jdbc:postgresql://db:5432/coworkhub
      DB_USER: postgres
      DB_PASS: postgres
      GITHUB_ID: ${GITHUB_ID:-changeme}
      GITHUB_SECRET: ${GITHUB_SECRET:-changeme}
    ports:
      - "8080:8080"

volumes:
  dbdata: